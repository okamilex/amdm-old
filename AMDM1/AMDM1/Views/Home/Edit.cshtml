
@{
    ViewBag.Title = "Edit";
}





@using (Ajax.BeginForm("SaveSong", new { songId = Model.Id }, new AjaxOptions { UpdateTargetId = "test" }))
{
    <input type="submit" class="btn btn-link" value="Submit" />
}

